# Environment variables for the frontend application
# Copy this file to .env.local and fill in your values

# Backend Secrets Service URL
# The service now uses Nginx reverse proxy for HTTPS termination
# For local development, use localhost
# For production, use your deployed EC2 instance URL with HTTPS (via Nginx)
VITE_SECRETS_SERVICE_URL=https://your-domain.com
VITE_SECRETS_SERVICE_USERNAME=admin
VITE_SECRETS_SERVICE_PASSWORD=changeme_strong_password

# Examples:
# Local development (HTTP, direct to Go app):
# VITE_SECRETS_SERVICE_URL=http://localhost:8080

# Production with domain (HTTPS via Nginx - recommended):
# VITE_SECRETS_SERVICE_URL=https://api.ethanmerrill.com

# Production with IP address (HTTPS via Nginx):
# VITE_SECRETS_SERVICE_URL=https://your-ec2-ip

# Note: Only variables prefixed with VITE_ are exposed to the browser
# See: https://vitejs.dev/guide/env-and-mode.html#env-variables
